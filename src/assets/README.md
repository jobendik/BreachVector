# Runtime Generated Assets

The current prototype intentionally uses generated Phaser textures so the game can run from a clean clone with no binary art or audio files.

Replace these placeholders later with sprite sheets, atlases, particle textures, UI frames, and audio files listed in `ASSETS.md`.
